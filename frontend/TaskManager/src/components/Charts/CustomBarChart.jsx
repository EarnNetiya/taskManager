import React from 'react'
import {
    BarChart,
    Bar,
    XAxis,
    YAxis,
    CartesianGrid,
    Tooltip,
    ResponsiveContainer,
    Legend,
    Cell,
} from "recharts";

const CustomBarChart = ({data}) => {
  return (
    <div>CustomBarChart</div>
  )
}

export default CustomBarChart